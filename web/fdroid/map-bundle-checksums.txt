# SHA-256 of the current offline map bundle.
#
# The rolling map-bundle-latest GitHub Release serves the mutable latest
# map-bundle.zip. android-release.yml verifies that download against this
# digest, then snapshots it per tag into the version's GitHub Release as
# map-bundle-<tag>.zip (the immutable, durable build input F-Droid's prebuild
# fetches).
#
# Update this single value ONLY when the map bundle is rebuilt
# (web/fdroid/scripts/publish-map-bundle.sh does it). This file is the one
# pinned source of truth: backend/scripts/fetch-fdroid-map-bundle.sh (the
# F-Droid prebuild) reads the digest from here at build time.
#
c85ed044fcc4ce1ed592757fe6f62be5d0b92cd04c88330b83e642b84023a205  map-bundle.zip
