<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Real-time GPS tracking — permitted Play use case. Used to ask the user
         to whitelist SrednaBG from battery optimization so OEM kill daemons
         don't stop the foreground service mid-trip. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- AA-disabled for the phone-only Play Store release.
         Restore for the AA follow-up release. -->
    <!-- <uses-permission android:name="androidx.car.app.NAVIGATION_TEMPLATES" /> -->
    <!-- <uses-permission android:name="androidx.car.app.ACCESS_SURFACE" /> -->

    <application
        android:name=".app.SrednaBGApp"
        android:allowBackup="true"
        android:appCategory="maps"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:localeConfig="@xml/locale_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.SrednaBG">

        <activity
            android:name=".app.ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.SrednaBG">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AA-disabled for the phone-only Play Store release.
             Restore for the AA follow-up release.
        <service
            android:name=".app.auto.SrednaBGCarAppService"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
                <category android:name="androidx.car.app.category.NAVIGATION" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.google.android.gearhead.vanagon.category.CATEGORY_NAVIGATION" />
            </intent-filter>
        </service>

        <meta-data
            android:name="androidx.car.app.minCarApiLevel"
            android:value="1" />
        -->

        <service
            android:name=".app.service.LocationTrackingService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- AA-disabled for the phone-only Play Store release.
             Restore for the AA follow-up release.
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />
        -->

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:authorities="${applicationId}"
                tools:node="remove" />
        </provider>

    </application>

</manifest>
